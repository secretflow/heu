#include "math/exceptions.h"

// Implementation is header-only, but we need this file for CMake
namespace bfv::math::rq {
// Empty implementation file - all exception classes are defined inline in
// header
}
